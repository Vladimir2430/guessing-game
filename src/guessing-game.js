class GuessingGame {
    constructor() {}

    setRange(min, max) {
        let n = min;
        let x = max;
        let l = false;
        let g = false;
    }

    guess() {
        if (let l) {
            x = a;
        } else if (let g) {
            n = a;
        }
      a=Math.ceil(n + ((x - n)/2));
      return a;
    }
    lower() {
       let l = true;
       let g = false;
    }

    greater() {
       let l = false;
       let g = true;
    }
}

module.exports = GuessingGame;
